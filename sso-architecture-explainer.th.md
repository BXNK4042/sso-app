# คำอธิบายไดอะแกรมสถาปัตยกรรม SSO

ไดอะแกรมนี้แสดงสถาปัตยกรรมการทำงานของระบบ SSO — เส้นทางการล็อกอินไหลผ่าน 8 คอมโพเนนต์ใน 3 โซน ดังนี้:

## Edge (ด้านบน)

- **Browser** เข้าทางพอร์ต `:9999` ไปที่ **Nginx Proxy** ซึ่งเป็นทางเข้าเดียว และแยกทราฟฟิกตาม path:
  - `/auth/` → Central Auth
  - `/lab/` → Web App 1
  - `/equipment/` → Web App 2

## App tier (ชั้นกลาง)

- **Central Auth** (กล่องสีส้ม — จุดศูนย์กลาง, FastAPI `:4000`) คือหัวใจของระบบ: ตรวจรหัสผ่านแล้วออก JWT cookie (`sso_token`)
- **Web App 1 · Lab** (`:5000`) กับ **Web App 2 · Equip** (`:5001`) ไม่ได้ตรวจรหัสผ่านเอง — แค่ตรวจลายเซ็น JWT ที่ Central Auth ออกให้ (เขียนไว้ใน sublabel จึงไม่มีลูกศรเชื่อมกัน)

## Auth data (ด้านล่าง)

- Central Auth ส่งชื่อผู้ใช้/รหัสผ่านไปตรวจกับ **FreeRADIUS** (`UDP 1812` — ลูกศรสีส้มคือเส้น auth หลัก)
- FreeRADIUS อ่านข้อมูลผู้ใช้จาก **PostgreSQL** (`radius :5432`) ผ่าน SQL
- **pgAdmin 4** (`:5050`, กล่องเส้นประ = เครื่องมือแอดมิน ไม่ได้อยู่ในเส้น request) ใช้จัดการ DB จากภายนอก

## คำอธิบายสัญลักษณ์

| สัญลักษณ์ | ความหมาย |
|---|---|
| สีน้ำเงิน | ทางเข้าภายนอก / HTTP |
| เส้นทึบ | เส้นทางภายใน |
| เส้นประ | ช่องทางเสริม (งานแอดมิน) |
| สีส้ม | ตัวกลาง SSO |

**สรุปประโยคเดียว:** *Nginx แยกเส้นทาง, Central Auth ยืนยันตัวตนผ่าน RADIUS/Postgres แล้วออก JWT, ทั้งสองแอปเชื่อ JWT นั้น*
